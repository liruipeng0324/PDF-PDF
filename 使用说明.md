# 使用说明

## 1. 工具用途

本工具用于制作“扫描版外观 + 可搜索文字层 + 原始书签”的双层 PDF。

适合处理：

- Word 招标文件、商务文件、技术文件
- 已经从 Word 导出的带书签 PDF
- 多个文件批量生成双层 PDF

最终文件可以在 PDF 阅读器里搜索文字，并保留目录书签。

## 2. 工作流程

默认流程如下：

1. Word 自动导出为带书签 PDF。
2. PDF 按 300 DPI 转为无损图片页。
3. 图片页合成为纯图片 PDF。
4. 自动打开 Adobe Acrobat Pro。
5. 自动进入“扫描和 OCR”并执行“识别文本”。
6. 等待 Acrobat 扫描完成并保存。
7. 验证最终 PDF 是否已有文字层。
8. 把原始 PDF 书签恢复到最终 PDF。
9. 输出 `文件名-dual-layer.pdf`。

如果输入本来就是“已导出的带书签 PDF”，第 1 步会跳过。

## 3. 首次安装

从 GitHub 下载项目后，先双击：

```text
一键安装组件.bat
```

它会自动处理：

- 安装项目内置 Python 3.12
- 安装 Python 依赖：`pikepdf`、`img2pdf`、`pypdfium2`、`pywin32`、`pywinauto`
- 下载并配置 Poppler
- 下载并配置 qpdf
- 检查并只补装缺失的组件
- 检查 Microsoft Word
- 检查 Adobe Acrobat Pro

注意：

- Microsoft Word 和 Adobe Acrobat Pro 需要你自己提前安装并激活。
- Adobe Reader 不可以替代 Acrobat Pro。
- Ghostscript 是可选组件，缺失时仍可以完成主流程中的其它步骤。
- 可以安全地重复运行一键安装脚本；已安装的 Python、Python 包、Poppler 和 qpdf 会显示 `[found]` 并跳过安装。
- 如需强制升级或重新安装，执行：`powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\install-components.ps1 -Force`。
- 如果公司网络无法访问 GitHub，安装脚本可能无法下载部分组件，需要换网络或手动准备组件。

## 4. 启动工具

双击：

```text
start-web.bat
```

正常情况下会自动打开本地网页。

如果浏览器没有自动打开，请看启动窗口里显示的本地地址，通常类似：

```text
http://127.0.0.1:8000
```

## 5. 推荐默认设置

一般不要改默认设置：

- 输入类型：Word 文档
- OCR 引擎：Acrobat 自动扫描
- 图片分辨率：300 DPI
- 图片格式：PNG / 无损中间图
- 渲染引擎：pdfium
- 压缩优化：开启
- 跳过已存在输出：开启

如果你输入的是已经导出的带书签 PDF，把“输入类型”改为：

```text
已导出的带书签 PDF
```

## 6. 单文件制作

1. 模式选择“单个文件”。
2. 输入类型选择“Word 文档”或“已导出的带书签 PDF”。
3. 点击“上传/选择”，选择输入文件。
4. 点击“选择目录”，选择输出文件夹。
5. 点击“开始制作”。

输出文件默认命名为：

```text
原文件名-dual-layer.pdf
```

## 7. 多文件队列

多文件队列用于一次处理多个 Word 或 PDF。

使用方法：

1. 模式选择“多文件队列”。
2. 点击“上传/选择”。
3. 在 Windows 文件选择器里一次选中多个文件。
4. 选择输出文件夹。
5. 点击“开始制作”。

队列会按顺序逐个处理。每个文件都会完整执行：

```text
Word/PDF 准备 -> 转图片 -> 合成图片 PDF -> Acrobat OCR -> 保存 -> 恢复书签 -> 输出
```

## 8. Acrobat OCR 期间不要操作什么

处理过程中 Acrobat 会自动打开、最大化、点击“扫描和 OCR”。

请不要：

- 手动关闭 Acrobat
- 手动移动 Acrobat 窗口
- 手动缩放 Acrobat 窗口
- 切换 Acrobat 工具栏布局
- 在 Acrobat 扫描中途点击其它工具

建议环境：

- 屏幕分辨率：1920 × 1080
- Windows 缩放：100%
- Acrobat Pro 中文界面

如果坐标点击不准，需要重新校准：

```text
ui_coords.json
```

## 9. 如何判断 Acrobat 正在扫描

工具会读取 Acrobat 窗口状态。

看到下面这类提示，就认为 Acrobat 仍在扫描：

```text
页面 180：将扫描的页面转换为可搜索的图像（请稍候）
```

注意：页码是变化的，不管是页面 2、页面 180，还是页面 1224，都不影响判断。

程序真正识别的是这些固定文字：

```text
将扫描的页面转换为可搜索的图像
请稍候
正在识别
正在执行页面识别
正在扫描
取消
```

如果检测到 Acrobat 已经在扫描，日志会显示：

```text
ACROBAT_UI_OCR_ALREADY_RUNNING wait_only
```

这表示程序只等待扫描完成，不会重复点击 OCR。

## 10. 大文件等待时间

大文件不能用固定 5 分钟判断是否结束。

现在等待时间会按页数自动增加：

```text
最长等待时间 = 页数 × 8 秒
最少 30 分钟
最多 4 小时
```

例如 1224 页会给到约 2 小时 43 分钟。

这只是最长等待上限。如果 Acrobat 提前扫完，程序会自动进入保存和书签恢复，不会等满。

## 11. 输出和日志

Web 界面右侧可以：

- 查看任务状态
- 打开输出位置
- 打开日志目录
- 查看当前执行到哪一步

常见输出：

```text
输出文件夹\原文件名-dual-layer.pdf
输出文件夹\_queue-logs
```

队列日志通常包括：

```text
item-001.log
item-002.log
summary-日期时间.csv
```

## 12. 清理缓存

Web 界面里的“清理缓存”只清理临时工作文件，不会删除已经生成的 PDF。

可以清理：

- 临时图片页
- 临时图片 PDF
- 队列临时日志
- Acrobat UI 截图调试文件

不要删除这些运行组件目录：

```text
tools\Python312
tools\poppler-26.02.0-0
tools\qpdf-12.3.2
```

## 13. 常见问题

### 13.1 Word 导出 PDF 失败

先手动打开 Microsoft Word，关闭：

- 激活提示
- 更新提示
- 文件恢复提示
- 受保护视图提示

然后重新运行工具。

如果仍失败，可以先在 Word 里手动另存为带书签 PDF，再用“已导出的带书签 PDF”模式处理。

### 13.2 Acrobat 没有开始扫描

检查：

- 是否安装 Adobe Acrobat Pro，而不是 Reader
- Acrobat 是否能手动打开“扫描和 OCR”
- Acrobat 是否有登录、更新、语言包、受保护视图弹窗
- 屏幕是否为 1920 × 1080、缩放 100%
- `ui_coords.json` 坐标是否仍然准确

### 13.3 扫描到一半像是重新扫描

新版本已经增加保护：

- 正在扫描时不会再次点击 OCR
- 会识别中文扫描提示
- 会按页数延长等待时间

如果仍然出现，请打开日志，看是否有：

```text
ACROBAT_UI_OCR_ALREADY_RUNNING wait_only
ACROBAT_UI_OCR_WAITING
ACROBAT_UI_OCR_COMPLETE
```

### 13.4 PDF 转图片阶段报 Python 模块错误

如果日志显示缺少 `pypdfium2`，说明那台电脑没有完整安装依赖。

处理方法：

```text
重新运行 一键安装组件.bat
```

或者手动执行：

```powershell
.\tools\Python312\python.exe -m pip install pypdfium2
```

### 13.5 文件体积比较大

300 DPI 扫描 PDF 本身会比较大，这是正常现象。

当前流程优先保证：

- 扫描清晰度
- OCR 准确性
- 书签保留

Acrobat 扫描后会进行压缩，程序最后也会尝试结构优化。

### 13.6 任务完成后没有书签

确认输入文件本身是否有书签。

如果输入是 Word，Word 标题样式需要正确，否则导出的 PDF 可能没有有效书签。

## 14. 组件清单

当前项目需要：

- Windows 10/11
- Microsoft Word
- Adobe Acrobat Pro
- Python 3.12
- pywin32
- pywinauto
- pikepdf
- img2pdf
- pypdfium2
- Poppler
- qpdf
- Ghostscript，可选但建议安装

一键安装脚本会尽量自动准备除 Word 和 Acrobat Pro 之外的组件。
