# PDF-PDF 安装到使用说明

## 1. 这个工具做什么

PDF-PDF 用来制作“双层 PDF”：

1. 把 Word 或带书签 PDF 转成页面图片。
2. 合成新的图片型 PDF。
3. OCR 识别文字，加入隐藏文字层。
4. 把原 PDF 书签加回最终 PDF。

最终效果：页面看起来像扫描件，但可以搜索文字，也保留书签。

## 2. 目录里哪些文件有用

常用入口：

- `start-web.bat`：双击启动网页工具。
- `setup-dependencies.ps1`：检查组件是否齐全。
- `build-portable-package.ps1`：制作便携 zip 包。
- `clean-cache.ps1`：只清理缓存。
- `clean-project.ps1`：安全清理测试输出、日志、缓存，默认不删组件和安装包。

核心脚本：

- `make-dual-pdf.ps1`：单个文件制作。
- `run-queue.ps1`：队列制作。
- `web-server.py`：本地网页服务。
- `tools\restore-bookmarks.py`：恢复书签。

功能组件：

- `tools\Python313`
- `tools\python-packages`
- `tools\poppler-26.02.0-0`
- `tools\qpdf-12.3.2`
- `tools\tesseract`
- `tools\tessdata`
- `tools\ghostscript-10.07.1`

不要删除 `tools` 目录，里面是运行需要的组件。

## 3. 第一次使用

进入项目目录：

```text
E:\Codex-Space\PDF&PDF
```

双击：

```text
start-web.bat
```

浏览器会打开：

```text
http://127.0.0.1:8765/
```

如果浏览器没有自动打开，可以手动复制上面的地址到浏览器。

## 4. 检查组件

网页顶部会显示组件检测结果。也可以手动运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\setup-dependencies.ps1
```

如果全部显示 `[found]` 或网页显示“可用”，说明组件齐全。

重要组件说明：

- Python：运行网页和 OCR 流程。
- Poppler pdftoppm：PDF 转 PNG。
- Tesseract OCR：OCR 识别。
- 中文 OCR 语言包：识别中文。
- Ghostscript：OCRmyPDF 处理 PDF 时需要。
- qpdf：PDF 工具链组件。
- Microsoft Word：只有直接处理 Word 文件时需要。

## 5. 单个文件制作

适合处理一个 Word 或一个已经导出的 PDF。

操作步骤：

1. 模式选择“单个文件”。
2. 输入类型选择：
   - “Word 文档”：直接处理 Word，需要电脑安装 Microsoft Word。
   - “已导出的带书签 PDF”：先从 Word 手动导出 PDF 后再处理。
3. 点击“上传/选择”，选择输入文件。
4. 点击“选择目录”，选择输出文件夹。
5. 选择 OCR 模式、语言、DPI。
6. 点击“开始制作”。

单个文件模式下，输出文件名会自动生成：

```text
原文件名-dual-layer.pdf
```

## 6. 文件夹队列制作

适合批量处理一个文件夹里的多个文件。

操作步骤：

1. 模式选择“文件夹队列”。
2. 输入类型选择 Word 或 PDF。
3. 点击“上传/选择”，选择输入文件夹。
4. 点击“选择目录”，选择输出文件夹。
5. 如果要处理子文件夹，勾选“包含子文件夹”。
6. 建议勾选“跳过已存在输出”。
7. 点击“开始制作”。

队列运行时会实时显示当前文件的每一步日志。

队列日志位置：

```text
输出目录\_queue-logs
```

## 7. CSV 队列制作

适合每个文件指定不同输出路径。

CSV 格式：

```csv
InputPath,OutputPath,Type
E:\Docs\a.pdf,E:\Docs\Output\a-dual-layer.pdf,pdf
E:\Docs\b.docx,E:\Docs\Output\b-dual-layer.pdf,word
```

字段说明：

- `InputPath`：输入 Word 或 PDF。
- `OutputPath`：输出 PDF 路径，可以留空。
- `Type`：`pdf`、`word` 或 `auto`。

网页中选择“CSV 队列”，再选择 CSV 文件和输出目录即可。

## 8. OCR 模式怎么选

推荐设置：

- 快速模式：速度优先，适合页数多、先看效果。
- 标准模式：日常推荐。
- 高精度模式：更慢，适合质量差或倾斜页面。

DPI 建议：

- 200 DPI：更快，文件更小。
- 300 DPI：推荐。
- 400 DPI：更慢，文件更大，只有确实需要时使用。

## 9. 取消任务

运行时点击“取消任务”。

现在取消会同时停止当前子进程，包括：

- Word 转 PDF 子任务
- `pdftoppm`
- OCR 相关进程

如果曾经手动强制关闭页面，仍然担心后台残留，可以打开任务管理器查看是否还有 `pdftoppm.exe`、`tesseract.exe`、`powershell.exe` 长时间运行。

## 10. 查看输出

任务完成后可点击：

- “打开输出位置”
- “打开日志目录”
- “查看最近输出”

如果是队列任务，汇总文件在：

```text
输出目录\_queue-logs\summary-时间.csv
```

## 11. 新电脑怎么用

推荐方式：制作便携包。

在已经配置好的电脑上运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\build-portable-package.ps1
```

生成 zip 后，把 zip 复制到新电脑，解压，双击：

```text
start-web.bat
```

注意：如果新电脑要直接处理 Word 文件，仍然需要安装 Microsoft Word。

## 12. 清理项目

只清理缓存：

```powershell
powershell -ExecutionPolicy Bypass -File .\clean-cache.ps1
```

安全清理测试输出、日志和缓存：

```powershell
powershell -ExecutionPolicy Bypass -File .\clean-project.ps1
```

默认不会删除安装包和 `tools` 功能组件。

只有明确要删除安装包时才使用：

```powershell
powershell -ExecutionPolicy Bypass -File .\clean-project.ps1 -RemoveInstallers
```

## 13. 常见问题

### 点击选择没有弹出窗口

刷新页面，或重新双击 `start-web.bat`。如果窗口在浏览器后面，查看任务栏。

### 依赖显示缺失

先点网页上的刷新按钮。也可以运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\setup-dependencies.ps1
```

### Word 处理失败

确认电脑安装了 Microsoft Word，并且能手动打开该 Word 文件。也可以先手动把 Word 导出为带书签 PDF，再选择“已导出的带书签 PDF”模式。

### OCR 很慢

页数多、DPI 高、中文识别都会慢。可以改成快速模式或 200 DPI。

### PDF 转 PNG 很慢

这是第 2 步，页数多时正常耗时较长。当前默认使用快速批量转换；如果勾选“显示 PNG 每页进度”，速度会更慢但进度更细。

### 最终 PDF 在哪里

单个文件：

```text
你选择的输出目录\原文件名-dual-layer.pdf
```

队列：

```text
你选择的输出目录\每个文件名-dual-layer.pdf
```

## 14. 建议工作方式

如果文档很多，建议：

1. 先用一个小文件测试。
2. 确认输出正常。
3. 再跑文件夹队列。
4. 勾选“跳过已存在输出”，方便中断后继续。
