# 使用说明

## 1. 工具用途

本工具用于把 Word 文档或已导出的带书签 PDF 制作成双层 PDF。

当前主流程是：

1. Word 自动导出为带书签 PDF。
2. PDF 按 300 DPI 渲染为无损图片页。
3. 图片页合成为纯图片 PDF。
4. 调用 Adobe Acrobat Pro 的桌面 OCR 功能生成可检索文字层。
5. 将原 PDF 书签恢复到最终 PDF。

最终文件既保留扫描版外观，也可以搜索文字，并带有目录书签。

## 2. 运行前要求

必须保留或安装：

- Python 运行环境：项目当前使用 `tools\Python312`。
- Microsoft Word：用于自动把 Word 导出为 PDF。
- Adobe Acrobat Pro：用于“扫描和 OCR”，Reader 不够用。
- Poppler / PDFium：用于 PDF 转图片。
- img2pdf：用于图片合成 PDF。
- pikepdf：用于恢复书签。
- qpdf / Ghostscript：用于最终结构压缩和兼容处理。

已经清理掉的根目录安装包不是运行组件，不影响使用。

## 3. 一键安装组件

第一次从 GitHub 下载项目后，先双击：

```text
一键安装组件.bat
```

这个脚本会自动处理：

- 下载并安装项目内置 Python 到 `tools\Python312`。
- 安装 Python 包：`pikepdf`、`pywin32`、`pywinauto`、`img2pdf`。
- 从 GitHub 下载 Poppler 和 qpdf。
- 尝试安装 Ghostscript。
- 检查 Microsoft Word 和 Adobe Acrobat Pro 是否可用。
- 运行最终依赖检查。

注意：

- Microsoft Word 和 Adobe Acrobat Pro 属于桌面商业软件，脚本只会检测和提示，不能替你安装授权软件。
- 如果公司网络或防火墙拦截 GitHub 下载，请手动准备组件后再运行 `setup-dependencies.ps1` 检查。
- 如果只想检查已有组件，不下载新文件，可以运行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\install-components.ps1 -SkipDownloads
```

## 4. 启动 Web 界面

双击：

```text
start-web.bat
```

浏览器会打开本地网页。如果没有自动打开，可以访问终端里显示的本地地址。

## 5. 推荐设置

普通使用建议保持默认：

- 输入类型：Word 文档
- OCR 引擎：Acrobat 自动扫描
- 图片分辨率：300 DPI
- 图片格式：PNG
- 压缩优化：开启
- 自动旋转：由 Acrobat 扫描时处理

Acrobat UI 自动化目前按 1920x1080、Windows 缩放 100%、Acrobat 最大化窗口设计。如果坐标失准，需要重新校准 `ui_coords.json`。

## 6. 单文件制作

1. 模式选择“单个文件”。
2. 输入类型选择“Word 文档”或“已导出的带书签 PDF”。
3. 点击“上传/选择”，选择输入文件。
4. 点击“选择目录”，选择输出文件夹。
5. 点击“开始制作”。

输出文件默认命名为：

```text
原文件名-dual-layer.pdf
```

处理过程中 Acrobat 会被自动打开、最大化、进入“扫描和 OCR”，扫描完成后会自动保存并继续恢复书签。

## 7. 多文件队列

多文件队列已经通过两份 Word 文件测试，结果为：

```text
Success: 2
Skipped: 0
Failed: 0
```

使用方法：

1. 模式选择“多文件队列”。
2. 点击“上传/选择”，一次选择多个 Word 或 PDF 文件。
3. 选择输出目录。
4. 点击“开始制作”。

队列会按顺序处理。每个文件都会完整执行：导出 PDF、渲染图片、Acrobat OCR、保存等待、恢复书签、输出最终 PDF。

## 8. CSV 队列

CSV 格式：

```csv
InputPath,OutputPath,Type
E:\PDF\INPUT\a.docx,E:\PDF\OUTPUT\a-dual-layer.pdf,word
E:\PDF\INPUT\b.pdf,E:\PDF\OUTPUT\b-dual-layer.pdf,pdf
```

字段说明：

- `InputPath`：输入 Word 或 PDF 文件路径。
- `OutputPath`：输出 PDF 路径，可以留空，由程序自动生成。
- `Type`：`word`、`pdf` 或 `auto`。

## 9. 输出和日志

Web 界面右侧提供：

- 打开输出位置
- 打开日志目录
- 查看最近生成 PDF
- 查看队列汇总

队列日志通常在：

```text
输出目录\_queue-logs
```

每个队列文件都有独立日志，例如：

```text
item-001.log
item-002.log
summary-日期时间.csv
```

## 10. Acrobat 扫描注意事项

Acrobat 自动扫描依赖桌面界面，因此处理时请注意：

- 不要手动关闭 Acrobat。
- 不要移动或缩放 Acrobat 窗口。
- 不要切换 Acrobat 工具栏布局。
- 屏幕建议保持 1920x1080，Windows 缩放 100%。
- 如果首次使用 Acrobat，请先手动打开一次，关闭登录、更新、保护视图、语言包提示等弹窗。

扫描完成后，程序会等待 Acrobat 真正把 OCR 结果写回临时 PDF，再进入书签恢复，避免复制到未扫描的旧图片 PDF。

## 11. 缓存清理

Web 界面中的“清理缓存”只清理临时工作目录和队列缓存，不会删除输出 PDF。

项目根目录中的安装包已经可以删除；运行所需组件目录不要删除，例如：

```text
tools\Python312
tools\poppler-26.02.0-0
tools\qpdf-12.3.2
```

## 12. 常见问题

### Word 导出失败

先手动打开 Microsoft Word，关闭激活、更新、文件恢复、受保护视图等提示。仍失败时，可以先在 Word 中另存为带书签 PDF，再选择“已导出的带书签 PDF”输入类型。

### Acrobat 没有开始扫描

确认 Acrobat Pro 已安装并可正常打开“扫描和 OCR”。如果界面位置变化，检查 `ui_coords.json` 中的坐标。

### 扫描完成后没有进入下一步

新版流程会等待 Acrobat 保存完成并检测文字层。如果日志停在保存阶段，通常是 Acrobat 尚未写回文件，或有保存/替换弹窗挡住了。

### 文件体积较大

300 DPI + 扫描图像 PDF 体积会比普通文字 PDF 大。Acrobat 扫描后通常会自动压缩，程序还会在最后尝试 qpdf 优化。
