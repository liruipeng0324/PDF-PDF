# 使用说明

## 1. 工具用途

本工具用于把 Word 或带书签 PDF 制作为图片版 PDF，并把原 PDF 的书签恢复到最终 PDF。

当前版本不生成隐藏文字层，只制作图片版 PDF 并恢复书签。

## 2. 启动

双击：

```text
start-web.bat
```

浏览器会打开本地网页。

## 3. 单个文件制作

1. 模式选择“单个文件”。
2. 输入类型选择 Word 文档或已导出的带书签 PDF。
3. 点击“选择文件”选择输入。
4. 点击“选择目录”选择输出位置。
5. 点击“开始制作”。

输出文件会保存到选择的输出目录，文件名通常为：

```text
原文件名-dual-layer.pdf
```

虽然文件名仍沿用旧命名，但当前内容是图片版 PDF。

## 4. 多文件队列

1. 模式选择“多文件队列”。
2. 选择多个 Word 或 PDF 文件。
3. 选择输出目录。
4. 点击“开始制作”。

工具会逐个处理文件，并在右侧显示队列日志。

## 5. CSV 队列

CSV 格式：

```csv
InputPath,OutputPath,Type
E:\Docs\a.pdf,E:\Docs\Output\a-dual-layer.pdf,pdf
E:\Docs\b.docx,E:\Docs\Output\b-dual-layer.pdf,word
```

字段说明：

- `InputPath`：输入 Word 或 PDF。
- `OutputPath`：输出 PDF 路径，可留空。
- `Type`：`pdf`、`word` 或 `auto`。

## 6. 依赖组件

需要保留：

- Python：运行网页和 PDF 脚本。
- Microsoft Word：自动导出 Word 为 PDF。
- PDFium / Poppler：PDF 转图片。
- img2pdf：图片合成为 PDF。
- pikepdf：恢复书签。
- Ghostscript / qpdf：压缩优化。

## 7. 输出位置

网页右侧有：

- 打开输出位置
- 打开日志目录
- 查看最近输出

如果任务失败，先打开日志目录查看对应日志。

## 8. 缓存清理

点击网页里的“清理缓存”，会清理临时工作目录和队列缓存，不会删除输出 PDF。

## 9. 常见问题

### Word 导出失败

请先手动打开 Word，关闭激活、更新、文件恢复、受保护视图等提示。仍失败时，可以先在 Word 里另存为 PDF，再选择 PDF 输入模式。

### PDF 转图片慢

页数多、DPI 高、页面内容复杂时会慢。当前默认使用 PDFium 快速渲染。

### 文件体积较大

这是图片版 PDF 的正常现象。可以保留“压缩优化”开启，让 Ghostscript 和 qpdf 尽量减小体积。
